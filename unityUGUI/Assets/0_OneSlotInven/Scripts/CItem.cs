using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
    이 아이템 클래스는
    '눈에 보이는 외관' + '실제 본질적인 데이터'
    의 조합으로 간주한다

    즉, 게임오브젝트가 가지는 메쉬 등 + Material + @가 외관, 즉 View
    mInfo가 실제 본질적인 데이터, 즉 Document
*/


public class CItem : MonoBehaviour
{
    public CItemInfo mInfo = null;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
